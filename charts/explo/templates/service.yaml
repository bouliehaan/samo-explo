apiVersion: v1
kind: Service
metadata:
  name: {{ include "explo.fullname" . }}
  labels:
    {{- include "explo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "explo.selectorLabels" . | nindent 4 }}
