.env
.DS_Store
/main
tmp/
.air.toml
logs/
/explo
src/web/dist/
src/web/frontend/node_modules/
/cache
/data/
/.data/
.zed/

# Generated by deploy.sh from your answers.
/docker-compose.yaml

# Maintainer tooling: deploy.sh ships a worktree to my dev boxes, commit.sh is
# the gate before anything goes public. They live on my machine, not in the repo.
#
# docker/start.sh is NOT mine — the Dockerfile COPYs it as the container's
# entrypoint, so ignoring it would build an image that cannot start.
*.sh
!docker/start.sh
